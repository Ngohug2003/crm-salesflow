# Database

The canonical ERD and rationale are in [architecture.md](architecture.md). PostgreSQL is the supported runtime database. ULIDs are used for domain records; package tables retain their package-supported key format.

